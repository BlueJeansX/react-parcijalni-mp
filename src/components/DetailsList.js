import React from 'react'

const DetailsList = (props) => {
    return (
        <>
            <p className="mt-4 text-center text-xl text-white"> Lista: </p>
            <ul>
                {
                   // props.podaci.map(rep => <li>{props.podaci.name}</li>)
                }
            </ul>

        </>
    )
}

export default DetailsList